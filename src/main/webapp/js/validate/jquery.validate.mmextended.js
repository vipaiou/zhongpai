$.validator.uniqueMediaName = function(value, element, param){
	return this.remote(value, element, param);
};

$.validator.uniqueAlias = function(value, element, param){
	return this.remote(value, element, param);
};

$.validator.parentChange = function(value, element, param){
	return this.remote(value, element, param);
}

$.validator.statusChange = function(value, element, param){
	return this.remote(value, element, param);
} 

$.validator.typeChange = function(value, element, param){
	return this.remote(value, element, param);
}

$.validator.uniqueUserName = function(value, element, param){
	return this.remote(value, element, param);
} 